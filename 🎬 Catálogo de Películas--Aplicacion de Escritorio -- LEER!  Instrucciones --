## 💻 Requisitos del Proyecto: Catálogo de Películas

- Python 3.11 o superior
- Sistema operativo: Windows, Linux o macOS
- Editor recomendado: VS Code o cualquier IDE compatible con Python

## 🛠️ Instalación y Ejecución

### 1. Clonar o descargar el proyecto

git clone https://github.com/tu_usuario/TP-CatalogoPeliculas.git
cd TP-CatalogoPeliculas

(O descargar y descomprimir el archivo .zip)

### 2. Crear un entorno virtual (opcional pero recomendado)

python -m venv nombre_del_entorno

✅ Reemplazá 'nombre_del_entorno' con un nombre como 'env' o 'myenv'

#### Para activar el entorno:

- En Windows (PowerShell):
  .\nombre_del_entorno\Scripts\Activate.ps1

- En Linux o macOS:
  source nombre_del_entorno/bin/activate

### 3. Instalar dependencias

pip install -r requirements.txt

✅ Este proyecto solo usa módulos estándar, por lo que no instala librerías externas.

### 4. Ubicarse en la carpeta del proyecto

cd ruta\al\proyecto\TP-CatalogoPeliculas

### 5. Ejecutar la aplicación

python main.py

✅ Se abrirá la ventana gráfica del catálogo de películas.

### 6. Desactivar el entorno virtual

deactivate

## 📦 Módulos estándar utilizados

- tkinter (Interfaz gráfica)
- sqlite3 (Base de datos local)

(No se requieren paquetes adicionales instalables con pip)

